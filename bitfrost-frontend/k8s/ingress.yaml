apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bitfrost-frontend-ingress
  namespace: ase-12
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure # send traffic over HTTPS
spec:
  ingressClassName: traefik
  rules:
    - host: bitfrost.sau-portal.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bitfrost-frontend-service
                port:
                  number: 80
