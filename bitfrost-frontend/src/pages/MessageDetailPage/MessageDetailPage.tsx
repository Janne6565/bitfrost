import { Box } from "@mui/joy";

const MessageDetailPage = () => {
  return (
    <Box
      sx={{
        display: "flex",
        height: "100%",
        flexGrow: 1,
        px: "3rem",
        flexDirection: "column",
      }}
    >
      <h1>test</h1>
    </Box>
  );
};

export default MessageDetailPage;
